const slider=document.querySelector(".compare-images"),curtain=slider.querySelector(".compare-images__slider");let curtainPlaceStart,clientX,sliderStyles=getComputedStyle(slider);function startTheCurtainShifting(t){curtainPlaceStart=+sliderStyles.getPropertyValue("--width"),clientX=t.clientX,window.addEventListener("pointermove",shiftТheСurtain)}function shiftТheСurtain(t){let e=t.clientX-clientX,i=curtainPlaceStart+e/slider.clientWidth,n=Math.min(Math.max(i,0),1);slider.style.setProperty("--width",`${n}`)}function stopTheCurtainShifting(){window.removeEventListener("pointermove",shiftТheСurtain)}window.addEventListener("pointerup",stopTheCurtainShifting),curtain.addEventListener("pointerdown",startTheCurtainShifting);